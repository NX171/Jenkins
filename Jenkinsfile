pipeline {
    agent any
    options {
        skipDefaultCheckout()
    }
    stages {
        stage("Building") {
            steps {
                script {
                    // This is a comment
                    echo "Building"
                }
            }
        }
    }
}
