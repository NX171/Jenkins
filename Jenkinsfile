pipeline {
    agent any
    options {
        skipDefaultCheckout()
    }
    stages {
        stage("Building") {
            script {
                echo "Welcome"
            }
        }
    }
}
